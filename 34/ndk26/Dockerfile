FROM smithmicro/android-build:34-gradle7.5

ENV ANDROID_NDK_VERSION=26.1.10909125

ENV ANDROID_NDK_HOME=$ANDROID_HOME/ndk/$ANDROID_NDK_VERSION

# Install the NDK
RUN sdkmanager "ndk;$ANDROID_NDK_VERSION"
